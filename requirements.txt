# Core deep learning
torch>=2.0
torchvision>=0.15

# Numerics / classical ML
numpy>=1.24
scipy>=1.10
scikit-learn>=1.2
pandas>=2.0

# Spatial transcriptomics / single-cell
scanpy>=1.10
anndata>=0.10

# Imaging
Pillow>=10.0

# Optional (uncomment as needed):
# openslide-python>=1.3      # level-0 WSI reading for on-the-fly cropping
# hest                       # HEST-1k loader + built-in patcher
# huggingface_hub>=0.20      # downloading HEST datasets
# timm>=0.9                  # UNI feature extractor
# matplotlib>=3.7            # evaluation/visualization
# scikit-misc>=0.3           # required only for HVG flavor="seurat_v3" (loess)

# Dev / testing
# pytest>=7.0
